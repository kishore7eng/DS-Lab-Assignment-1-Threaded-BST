First of  all create a file named new.txt in any location and copy the path of the file and paste it in fopen_s command of the program.

Make sure graphviz is installed on your system.

just do build and run and enter your choice of which operation you need to perform.

After deletion of an element the new tree will be considered for further operations.